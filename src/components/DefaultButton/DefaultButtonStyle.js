import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    defaultInput: {
        backgroundColor: "#01633D",
        padding: 15,
        borderRadius: 15
    },
    textButton: {
        fontWeight: "bold",
        fontSize: 20,
        color: "white",
        textAlign: "center",
    }
})